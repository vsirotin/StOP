# Copilot Instructions for aiavivo

## Commit workflow

When the user says **"Make commit"**, run the script `scripts/make-commit.sh` from the workspace root.
This script stages all changes, commits using `commit-text-proposal.txt`, and pushes to origin.
