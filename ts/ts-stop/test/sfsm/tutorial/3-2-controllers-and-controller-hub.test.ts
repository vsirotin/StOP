import { Sfsm, FaDefinition, ICommandReceiver, TransceiverHub, ISignalSender } from "../../../src/sfsm";



describe.skip("Tutorial – Controllers and the Transceiver Hub (turnstile gate)", () => {
    it.skip("should return an object for the extended FA file", () => {
            const a = 2 + 2;
            expect(a).toBe(4);
        });
});
