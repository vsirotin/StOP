{
  "name": "@vsirotin/ts-example",
  "version": "1.0.0",
  "description": "TypeScript example for @vsirotin/ts-stop",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@vsirotin/ts-stop": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
