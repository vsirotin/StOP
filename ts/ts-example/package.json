{
  "name": "@vsirotin/ts-example",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start",
    "clean": "rm -rf dist",
    "update:local": "cd ../ts-stop && npm run publish:local"
  },
  "dependencies": {
    "@vsirotin/ts-stop": "0.0.1"
  }
}
