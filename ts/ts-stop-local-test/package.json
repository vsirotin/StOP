{
  "name": "ts-stop-local-test",
  "version": "1.0.0",
  "description": "Local integration test project for @vsirotin/ts-stop package",
  "private": true,
  "scripts": {
    "test": "node src/test-imports.js",
    "test:bundle": "esbuild src/test-bundle.ts --bundle --outfile=dist/bundle.js --platform=browser --external:@vsirotin/ts-stop"
  },
  "dependencies": {
    "@vsirotin/ts-stop": "file:../ts-stop"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
